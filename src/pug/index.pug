extends base

block content
  include components/form/form 
  include components/cards/cards

  .cards
    section.cards__column
      +wishes()
      +registration()
    setion.cards__column
      +book-a-room({price: 0, number:"888", kind:"ЛЮКС"})
      +enter()      
    section.cards__column
      +room-detail({price: 9990, number:"888", kind:"ЛЮКС"})      

