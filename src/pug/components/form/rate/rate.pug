mixin rate(options)
  fieldset.rate
    if options.legend
      legend.rate__header= options.legend
    .rate__group
      each star in options.items.reverse()
        input.rate__star(type="radio")&attributes(star)
        