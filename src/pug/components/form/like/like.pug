mixin like(options)  
  -
    const _class = options.state.checked ? "like__input_active" : ""
  button.like__input(class=_class)&attributes({"data-type-button":"like"})
    span.like__favorite
      |favorite
    span.like__text=options.value    
